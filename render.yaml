services:
  - type: web
    name: uni-cse-edupulse
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cloud_server:app
    envVars:
      - key: PORT
        value: 8080
